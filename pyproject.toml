[project]
name = "opencode"
version = "0.1.0"
description = "PDF Q&A with AppHub LLM API, running in Docker"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "openai>=1.40,<2.0",
    "pypdf>=4.0,<5.0",
]
