[project]
name = "beehub-agent"
version = "0.1.0"
description = "BEEHub transform agent: CSV + paradigm notes -> BIDS-compliant BEEHub project"
requires-python = ">=3.12"
dependencies = [
    "openai>=1.40,<2.0",
    "pandas>=2.2,<3.0",
]
